import React, { useState, useEffect } from "react";
import APIService from "./services/APIService";

const gitToken = "";
const cache = new Map();

const TagsInfo = ({ owner, repoInfo }) => {
  return (
    <div>TODO</div>
  );
};

const TagsContent = ({ reponame, owner }) => {
  return <TagsInfo />
};

export default TagsContent;