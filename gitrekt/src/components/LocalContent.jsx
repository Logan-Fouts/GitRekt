import React, { useState, useEffect } from "react";
import APIService from "./services/APIService";

const cache = new Map();

const LocalInfo = ({ owner, repoInfo }) => {
  return <div>TODO</div>;
};

const LocalContent = ({ reponame, owner }) => {
  return <LocalInfo />;
};

export default LocalContent;
